import axios from '../../axios'

//（省端）获取新增科室及对应编辑项
export const getEditList = (data) => {
  return axios({
      url: '/administrativeOffice/getEditableItem',
      method: 'post',
      data
  })
}

//（省端）获取科室列表
export const getDeskList = (data) => {
  return axios({
      url: '/administrativeOffice/getList',
      method: 'post',
      data
  })
}

// （省端）保存配置
export const saveConfig = (data) => {
  return axios({
      url: '/administrativeOffice/save',
      method: 'post',
      data
  })
}
